{
  "name": "editorconfig-build-action",
  "version": "2.0.0",
  "private": true,
  "description": "GitHub Action to validate or sync .editorconfig against editorconfig.build",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "diff": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.5",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
